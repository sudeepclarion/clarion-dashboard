# Clarion API (backend)
# Dev: backend on :5080. Production: your EC2/ALB URL including /api
VITE_CLARION_API_URL=http://localhost:5080/api

# Auth: sign in via the dashboard login page (JWT). Users are created in clarion-internal-dashboard.
# Optional legacy bypass (must match CLARION_API_SECRET) — prefer login instead:
# VITE_CLARION_API_SECRET=
